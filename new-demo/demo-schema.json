# feed through jq -jS | jq -sR to sort and escape quotes and newlines
{
  "storage": {
		"aliases": {
			"svc-name": {
				"type": "string",
				"pattern": "^(?:[a-z0-9]+-?)*[a-z](?:-?[a-z0-9])*$"
			}
		},
		"schema": {
      "statuses": {
				"keys": "$svc-name",
				"values": {
					"type": "string",
					"choices": ["up", "down"]
				}
      },
			"snaps": {
				"keys": "$svc-name",
				"values": {
					"schema": {
						"revision": {
							"type": "int",
							"min": 1
						}
					}
				}
			}
    }
  }
}
